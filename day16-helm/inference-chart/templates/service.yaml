apiVersion: v1
kind: Service
metadata:
  name: {{ include "inference-chart.fullname" . }}
  labels:
    {{- include "inference-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "inference-chart.selectorLabels" . | nindent 4 }}
