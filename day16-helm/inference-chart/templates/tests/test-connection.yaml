apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "inference-chart.fullname" . }}-test-connection"
  labels:
    {{- include "inference-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "inference-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
