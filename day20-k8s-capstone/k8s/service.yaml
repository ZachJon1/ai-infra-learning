apiVersion: v1
kind: Service
metadata:
  name: inference-service
  namespace: inference

spec:
  selector:
    app: inference-api

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080

  type: ClusterIP
