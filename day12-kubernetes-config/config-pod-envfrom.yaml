apiVersion: v1
kind: Pod
metadata:
  name: config-demo-envfrom
spec:
  containers:
    - name: demo
      image: alpine:3.20
      command: ["sh", "-c", "sleep 3600"]

      envFrom:
        - configMapRef:
            name: inference-config

        - secretRef:
            name: inference-secret
