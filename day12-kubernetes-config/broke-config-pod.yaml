apiVersion: v1
kind: Pod
metadata:
  name: broken-config-demo
spec:
  containers:
    - name: demo
      image: alpine:3.20
      command: ["sh", "-c", "sleep 3600"]

      env:
        - name: MODEL_VERSION
          valueFrom:
            configMapKeyRef:
              name: inference-config
              key: MODEL_VERSION
